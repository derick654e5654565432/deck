{
  "name": "deck",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Deck — Deric's personal project tracker. Where each thing you're carrying lives.",
  "scripts": {
    "start": "node server.js",
    "hash": "node scripts/hash-password.mjs",
    "build:frontend": "cd frontend && npm install && npm run build",
    "dev:server": "node --watch server.js"
  },
  "dependencies": {
    "express": "^4.21.2",
    "express-session": "^1.18.1",
    "multer": "^2.2.0"
  }
}
